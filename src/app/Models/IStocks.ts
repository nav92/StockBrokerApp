export interface IStocks {
    clientId: string;
    clientName: string;
    stocksName: string;
    quantity: number;
    purchaseDate: string;
    purchasePrice: number;
    currentMarketRate: number;
}